using UnityEngine;

// public class Background : MonoBehaviour
// {
//     public Material bgMaterial; // 배경 매터리얼
//     public float scrollSpeed = 0.2f; // 스크롤 속도
    
// void Update()
//   {
//         Vector2.direction = Vector2.up; // 방향이 필요하다.
//         bgMaterial.mainTextureOffset += direction * scrollSpeed * Time.deltaTime; // 스크롤 P = P0 + vt , offset 정보를 Vector2 단계로 가지고 있음.
//   }
// }
